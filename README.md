# SecurOps-Linux-Auditor
A Python or Bash script that an administrator runs on a Linux Server. It scans the server for security issues (e.g., weak file permissions, open ports, status of firewalls, checking if SSH root login is disabled) and automatically fixes them or generates a neat report.
